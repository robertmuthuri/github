import { RepoHoverDirective } from './repo-hover.directive';

describe('RepoHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new RepoHoverDirective();
    expect(directive).toBeTruthy();
  });
});
